paths-ignore:
  # This exact file contains synthetic placeholders used by local redaction tests.
  # The local security gate still scans it; no directory-wide exclusion is allowed.
  - "security/redaction-regression-cases.json"
